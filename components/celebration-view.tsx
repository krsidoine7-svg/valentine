"use client"`n`nimport { useEffect, useCallback } from "react"`nimport confetti from "canvas-confetti"`n`nexport function CelebrationView() {`n  const triggerConfetti = useCallback(() => {`n    const duration = 5 * 1000`n    const animationEnd = Date.now() + duration`n    const defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 }`n`n    const randomInRange = (min, max) => Math.random() * (max - min) + min`n`n    const interval = setInterval(function () {`n      const timeLeft = animationEnd - Date.now()`n`n      if (timeLeft <= 0) {`n        return clearInterval(interval)`n      }`n`n      const particleCount = 50 * (timeLeft / duration)`n      confetti({`n        ...defaults,`n        particleCount,`n        origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 },`n      })`n      confetti({`n        ...defaults,`n        particleCount,`n        origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 },`n      })`n    }, 250)`n  }, [])`n`n  useEffect(() => {`n    confetti({`n      particleCount: 150,`n      spread: 70,`n      origin: { y: 0.6 },`n      colors: ["#ff0000", "#ff69b4", "#ff1493", "#db2777"],`n    })`n    triggerConfetti()`n  }, [triggerConfetti])`n`n  return (`n    <div className="flex flex-col items-center justify-center min-h-[400px] w-full max-w-lg mx-auto text-center p-8 animate-fade-in-up">`n      <div className="relative mb-8 transform hover:scale-110 transition-transform duration-300">`n        <span className="text-8xl sm:text-9xl block animate-bounce" role="img" aria-label="Heart"></span>`n        <div className="absolute -top-4 -right-4 animate-pulse"><span className="text-4xl"></span></div>`n      </div>`n      <h1 className="font-dancing text-5xl sm:text-6xl text-primary mb-6 drop-shadow-sm">Yesssss !</h1>`n      <p className="text-2xl sm:text-3xl font-medium text-foreground mb-4">Je savais que tu dirais oui ! </p>`n      <p className="text-lg text-muted-foreground italic max-w-xs mx-auto">Prépare-toi pour une Saint-Valentin inoubliable... </p>`n      <div className="mt-10 flex gap-2 justify-center">`n        {["", "", "", ""].map((emoji, i) => (`n          <span key={i} className="text-3xl animate-bounce" style={{ animationDelay: `${i * 0.2}s` }}>{emoji}</span>`n        ))}`n      </div>`n    </div>`n  )`n}